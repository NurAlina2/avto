<Window x:Class="avto.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:avto"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="23*" />
            <RowDefinition Height="200*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Background="Beige" Orientation="Horizontal">
           

            <Button x:Name="BackBtn" Click="BackBtn_Click"
                    Width="150" Height="30" FontSize="17"
                    
                    Margin="400 0 0 0" FontFamily="Tahoma" >НАЗАД</Button>
            <Button x:Name="ExitBtn"
                     Height="30" FontSize="17"
                   Width="100" 
                    Margin="20 0 0 0" FontFamily="Tahoma">Выход</Button>

        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
          
        </StackPanel>
        <Frame x:Name="FrameMain" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
